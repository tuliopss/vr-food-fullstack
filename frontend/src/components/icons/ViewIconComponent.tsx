import React from "react";
import VisibilityIcon from "@mui/icons-material/Visibility";
type Props = {};

const ViewIconComponent = (props: Props) => {
  return <VisibilityIcon />;
};

export default ViewIconComponent;
